Desafio MVT de Gonzalo Garcia

>> Consigna: Crear una web que permite ver los datos de algunos de tus familiares, guardados en un BD.

1. Deberá tener un template, una vista y un modelo (como mínimo, pueden usar más)
    tiene un template llamado "listado_personas"
    tiene una vista llamada "listado_familiares"
    tiene un modelo llamado "Persona"

2. La clase del modelo, deberá guardar mínimo un número, una cadena y una fecha (puede guardar más cosas)
    en la clase del modelo guarda:
        un número: llamado "dni"
        una cadena: llamada "nombre"
        una fecha: llamada "nacimiento"

3. Se deberán crear como mínimo 3 familiares
    en la base de datos hay creados 3 instancias de la clase "Persona"

4. Los familiares se deben ver desde la web.
    la lista de familiares se puede ver desde la url "app1/familiares.html"
